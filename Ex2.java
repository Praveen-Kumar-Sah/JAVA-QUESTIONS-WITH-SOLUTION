// package Full for java prog;

public class Ex2 {
    public static void main(String args[]) {
        int a = 6; // initializing and declearing of variable
        int b = 9;
        int m = a & b;
        int sum = 0, sub = 0, mult = 0;
        float div = 0;
        double divd = 0;
        sum = a + b;
        sub = b - a;
        mult = a * b;
        div = b / a;
        divd = b / a;
        System.out.println(sum + "\n" + sub + "\n" + mult + "\n" + div + "\n" + divd + "\n" + m);
    }
}
